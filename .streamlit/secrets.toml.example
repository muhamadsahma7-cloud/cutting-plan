# Copy this file to .streamlit/secrets.toml (which is gitignored) and fill in
# your own values, or leave it out entirely — app.py falls back to the same
# public anon key already used by the Netlify app if these aren't set.

SUPABASE_URL = "https://qaiwtwyvnrquwsatnffv.supabase.co"
SUPABASE_ANON_KEY = "your-anon-key-here"
